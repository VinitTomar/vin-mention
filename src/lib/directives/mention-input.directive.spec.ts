import { MentionInputDirective } from './mention-input.directive';

describe('MentionInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionInputDirective();
    expect(directive).toBeTruthy();
  });
});
