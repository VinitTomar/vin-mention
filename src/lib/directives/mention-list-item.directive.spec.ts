import { MentionListItemDirective } from './mention-list-item.directive';

describe('MentionListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionListItemDirective();
    expect(directive).toBeTruthy();
  });
});
